# 2026 年 9 月复盘

对应 [AI Agent 职业规划手册](../../README.md) 第十六节复盘表格式。

| 项目 | 本月记录 |
|---|---|
| 本月学习的 3 个关键知识 | 1. Agent Loop 的核心是"思考→工具调用→结果回填"的循环，记忆就是不断增长的消息数组；2. MCP 把工具的描述和调用标准化，让工具可以脱离具体 Agent 进程被发现和复用；3. A2A 的 Agent Card 解决的是"整个 Agent 的能力怎么被发现"，和 MCP 解决"单个工具怎么被发现"是同一模式在不同粒度上的重复 |
| 本月完成的 Demo / Repo | agent-lab 仓库：`01-basic`（最小 Agent Loop）、`02-tool-calling`（多工具选择与链式调用）、`03-mcp`（真实跨进程 MCP client/server）、`04-a2a`（Agent Card 发现 + JSON-RPC 消息）全部跑通并提交 |
| 本月发布的内容 | `docs/articles/2026-09-agent-loop-tool-calling-mcp-a2a.md`（技术总结）、`docs/articles/2026-09-debugging-notes.md`（踩坑笔记） |
| 本月新增用户 / 开发者 | 0（本月是个人验证阶段，未对外发布） |
| 本月收入 | 0 元 |
| 本月协议/标准动态记录（V2 新增） | 本月未做专门的协议趋势扫描，只在实操中确认了 MCP TypeScript SDK 当前稳定版是 1.x（`@modelcontextprotocol/sdk`），A2A JS SDK 已发布 v1.0 稳定版（`@a2a-js/sdk`），两者官方 SDK 都可直接生产可用，不用等 |
| 遇到的最大问题 | 工程兼容性问题比协议理解本身更耗时：第三方 LLM 代理对不同模型的模型命名、渠道权限、流式行为都不一致，调试这些花的时间比理解 Agent Loop/MCP/A2A 概念本身还多 |
| 下个月唯一最重要目标 | 完成 Agent Passport v0.1：搞懂 ERC-8004、EIP-712、EIP-1271、Wallet、ENS，能独立解释 Agent Identity 是怎么工作的 |

## 补充说明

- 四个 Demo 都用了真实的官方 SDK（`openai`、`@modelcontextprotocol/sdk`、`@a2a-js/sdk`），不是概念性的伪代码，符合手册第二条原则"每学一个技术必须留下可运行 Demo"
- 本月还没有进行 Hackathon/Grant 相关的 30 分钟浏览动作（手册二十三节要求），补记为待办，10 月一起处理
