Agent 基础篇的三大支柱：

1. Tool Calling / Function Calling —— 模型自己判断该不该调用工具、调用哪个工具、传什么参数。
2. Agent Loop —— 思考、行动、观察结果，循环直到能给出最终答案。
3. Memory —— 至少要能维护当前对话的上下文，不然每一步都是失忆重来。

这三者组合起来，才是一个"看起来像 Agent"的东西，而不是一次性问答。
